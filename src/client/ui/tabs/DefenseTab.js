var DefenseTab = function (mapObj) {

    this.mapObj = mapObj;
    this.content= $('<div id="defenseTab">List of defense Upgrades that can be build. List of Defense Upgrades that were already build. List of Units assigned in defense squad.</div>');

}

